[package]
name = "watch"
version = "0.1.0"
authors = ["ander <ander.ender@gmail.com>"]
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
chrono = "0.4"
termion = "4"

[profile.release]
lto = true
